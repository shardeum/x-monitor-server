const countedEvents = require('./countedEvents');

export const Controller = {
  joining,
  joined,
  active,
  removed,
  heartbeat,
  report,
  flush,
  syncReport,
  getSyncReports,
  getTxCoverage,
  history,
  getScaleReports,
  mock,
  getRemoved,
  rareCounter,
  resetRareCounter,
  countedEvents,
};
